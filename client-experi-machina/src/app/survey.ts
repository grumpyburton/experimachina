export interface Survey {
  id: number;
  name: string;
  description: string;
  code: string;
  active: boolean;
  createDate: string;
  updateDate: string;
  startDate: string;
  endDate: string;
  expireDate: string;
}

